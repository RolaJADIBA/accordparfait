<form method="post" action="{{ path('jardin_delete', {'id': jardin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jardin.id) }}">
    <button class="btn btn-danger btn-sm mt-3">Effacer</button>
</form>
