{% extends 'base_admin.html.twig' %}

{% block title %}Modifier Delf{% endblock %}

{% block body %}
<div class="container">
    <div class="row mt-3">
        <div class="col-md-8">

            {{ include('admin/delf/_form.html.twig', {'button_label': 'Modifier'}) }}

            <a href="{{ path('delf_index') }}" class="btn btn-warning btn-sm text-white">retour à la liste</a>

            {{ include('admin/delf/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
