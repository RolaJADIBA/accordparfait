{% extends 'base_admin.html.twig' %}

{% block title %}Modifier le événement{% endblock %}

{% block body %}
<div class="container">
    <div class="row mt-3">
        <h3>Modifier l'événement: {{evenement.titre}}</h3>

        {{ include('admin/evenements/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="col-md-2">
            <a href="{{ path('evenements_index') }}" class="btn btn-warning btn-sm text-white">retour à la liste</a>
        </div>

        {{ include('admin/evenements/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}
