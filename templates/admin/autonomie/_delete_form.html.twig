<form method="post" action="{{ path('autonomie_delete', {'id': autonomie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autonomie.id) }}">
    <button class="btn btn btn-danger btn-sm mt-3">Effacer</button>
</form>
