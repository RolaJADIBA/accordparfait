{% extends 'base_admin.html.twig' %}

{% block title %}Edit Autonomie{% endblock %}

{% block body %}
<div class="container">
    <div class="row mt-3">
        <div class="col-md-8">

            <h1>Modifier {{autonomie.titre}}</h1>

            {{ include('admin/autonomie/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('autonomie_index') }}" class="btn btn-warning btn-sm text-white">retour à la liste</a>

            {{ include('admin/autonomie/_delete_form.html.twig') }}

        </div>
    </div>
</div>

{% endblock %}
