{% extends 'base_admin.html.twig' %}

{% block title %}Edit Complement{% endblock %}

{% block body %}
<div class="container">
    <div class="row mt-3">
        <div class="col-md-8">

            <h1>Modifier Complement</h1>

            {{ include('admin/complement/_form.html.twig', {'button_label': 'Modifier'}) }}

            <a href="{{ path('complement_index') }}" class="btn btn-warning btn-sm text-white">retour à la liste</a>

            {{ include('admin/complement/_delete_form.html.twig') }}
        </div>
    </div>
</div>

{% endblock %}
