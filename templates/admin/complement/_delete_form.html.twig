<form method="post" action="{{ path('complement_delete', {'id': complement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ complement.id) }}">
    <button class="btn btn-danger btn-sm mt-3">Effacer</button>
</form>
