{% extends 'base.html.twig' %}

{% block title %}modifier mon compte{% endblock %}

{% block body %}
<div class="container">
    <div class="row mt-3">
        <div class="col-md-8">

            <h3>modifier mon compte</h3>

            {{ form_start(form) }}
                {{ form_widget(form) }}

                <p><a href="{{ path('app_forgot_password_request') }}">changer votre mot de passe</a></p>

                <button class="btn btn-secondary btn-sm mt-3 mb-3">{{ button_label|default('enregistrer') }}</button>
            {{ form_end(form) }}

        </div>
    </div>
</div>
{% endblock %}
